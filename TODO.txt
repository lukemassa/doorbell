- Implement test procedure
  - Double press unit 1, get a notification from nfty that test has begun
  - Enters 1 minute period where all normal notification is bypassed, only notify via nfty and only notify me.
- Unit tests
- fan control
    turn on fan if it gets too hot
    have different thresholds during day vs night
- Tell systemd we're good
- Split ring into "on_press" vs "publish"
- Use a different client for local notifications
- Figure out create_mqtt_notifier_clients_on_startup
